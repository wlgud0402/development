1. filezilla 들어감 

2. 호스트에 내가 올릴곳 ip 번호 (ex:집 서버= 192.168.0.13) aws의 ip등

3.사용자명 wlgud 비번 04020402 포트:22(원격접속, 원격파일전송)
    ->빠른연결

4.서버에 내 폴더를 찾아서 넣어줌(작업한거, 서비스할거)

5.putty 접속

6.Host Name (or IP address)에는 서버나 아마존꺼 포트 22

7.putty 가 켜짐

8.리눅스에는 python2.7하고 3버전 둘다 있으므로 구분해주기

9. python3 -m venv .venv

10. cd .venv >> cd bin >> source activate >> cd .. >> cd ..

11.pip install flask

12.python을 실행하는 것에는 권한이 필요하므로 슈퍼유저로 실행

13.sudo python3 (파일명).py

(어떤 과정을 할때는 pwd로 자신의 위치를 잘 파악하고 ls, ls -al 등으로 존재하는 폴더, 파일등을 잘 파악)

(vi로 고칠때도 cd를 통해 그 파일이 있는 폴더로 일단 들어가고 그후에 vi server.py OR vi main.html 등을 통해 고칠 수 있다.)